# 第一页视觉精修计划

根据您的反馈，我们将对第一页进行精细化调整，使其视觉效果更加自然和谐。

## 调整目标
1.  **精准对齐粒子**: 缩小粒子动效的范围（半径），并重新调整位置，使其紧密贴合右上角的星球，不再显得“散漫”。
2.  **移除遮罩**: 删除背景图上的黑色蒙版，还原插画原本的色彩与质感。
3.  **文字融入**: 调整文字样式，通过降低不透明度或增加柔和的阴影，使其在无遮罩的情况下依然清晰，且呈现出“原生嵌入”的视觉效果。

## 具体实施步骤

### 1. 修改 `src/pages/Page1.jsx`
*   **移除蒙版**: 删除 `<div className="absolute inset-0 bg-black/30" />` 代码行。
*   **调整粒子参数 (`ParticleSphere`)**:
    *   **减小半径 (`radius`)**: 从 `1.2` 调整为 `0.7` 左右，使其更紧凑。
    *   **微调位置 (`position`)**: 尝试将位置调整为 `[3.5, 2.2, 0]` (向右上方移动)，以更精准地匹配插画中的星球位置。
    *   **调整粒子密度与大小**: 稍微减小粒子尺寸 (`size`)，增加细腻度。
*   **优化文字样式**:
    *   将文字颜色调整为 `text-white/80` 或 `text-gray-100`，降低刺眼的对比度。
    *   添加 `drop-shadow-md` 或 `shadow-black/20`，保证文字在浅色背景区域的可读性，同时保持“原生感”。

### 2. 文档更新 (`FORTrae.md`)
*   记录此次去除遮罩与粒子精调的视觉优化思路。

请确认是否执行此优化方案？
